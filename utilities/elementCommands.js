/**
 * https://webdriver.io/docs/api/browser/addCommand 
 * File for adding helper commands on element level
 * Command is accessible in tests and screens on $ object
 * e.g. $(some_id).getUser()
 * */
module.exports = {
    examplECommand: () => console.log('Example Command')
}
