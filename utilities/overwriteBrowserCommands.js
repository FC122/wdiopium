/**
 * https://webdriver.io/docs/api/browser/overwriteCommand/
 * File for overwriting existing commands accessible on the browser object
 * Command is accessible in tests and screens on browser object
 * e.g. browser.pause()
 * */
module.exports = {
    exampleCommand: (originalFn) => {/**Custom Implementation */ }
}
